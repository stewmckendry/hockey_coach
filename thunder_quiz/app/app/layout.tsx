import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Thunder Hockey Quiz | U10A Ted Reeve Thunder",
  description: "Test your hockey knowledge with the Thunder Hockey Quiz! Designed for U10A Ted Reeve Thunder players.",
  openGraph: {
    title: "Thunder Hockey Quiz",
    description: "Interactive hockey quiz for U10A Ted Reeve Thunder team",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
